package com.example.officerbumble.interfaces;

import com.example.officerbumble.gameentities.Chickenator;

public interface ChickenatorListener {
	public void ThrowFeather(Chickenator _chickenator, int _featherCount, float _currentTimeMilliseconds);
}
